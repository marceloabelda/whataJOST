{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "uptime-kuma-window",
  "description": "Allow the Uptime Kuma config window to invoke config commands",
  "windows": ["uptime-kuma-config"],
  "permissions": [
    "core:default",
    "allow-whatsapp-ipc"
  ]
}
