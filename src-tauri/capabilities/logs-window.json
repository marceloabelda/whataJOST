{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "logs-window",
  "description": "Allow the logs viewer window to invoke log commands",
  "windows": ["logs"],
  "permissions": [
    "core:default",
    "allow-whatsapp-ipc"
  ]
}
